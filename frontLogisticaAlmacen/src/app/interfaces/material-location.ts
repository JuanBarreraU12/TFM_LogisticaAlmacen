export interface MaterialLocation {
    id?: Number,
    materialId: Number,
    material?: String,
    image?: String,
    price?: Number,
    sku?: String,
    locationId: Number,
    location?: String,
    stock?: Number
}
